# Audit bundles

A **bundle** is a folder a human can audit end-to-end — the exact artifacts the
`prior-generator` CLI writes. The page below is generated from the
`prior_generator.bundles` module docstring and its two public functions, so it
documents precisely what lands on disk.

!!! note "See it in action"
    The [Audit bundles guide](../guide/bundles.md) writes a real bundle during
    the docs build and shows every figure inline.

::: prior_generator.bundles
    options:
      show_root_heading: true
      show_root_toc_entry: false
      heading_level: 2
